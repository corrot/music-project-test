# music-project-test

How to use:

1. Prerequisites
   install git (https://git-scm.com/downloads)
   install node.js (https://nodejs.org/en/download/)

2. Clone
   right-click on desktop > git bash here;
   paste this: git clone https://github.com/corrot/music-project-test.git
   stop looking at the screen like a fuckhead and hit the ENTER, dude!

Life Cycle:

1. right-click inside project folder > git bash here;

2. git pull;

3. open the project file and do whatever the fuck you think you wanna do;

4. git add . (this command is used to stage current changes for commiting);

5. git commit -m "your comment" (this commits current changes to local branch)
   please, make sure to structure your comment like this: "add vocal track" or "delete garbage tracks" or "fucked up whole shit"
   see the pattern? use it!

6. git push (this pushes current commit to remote branch so that others can view and edit your shit);

7. if there are merge conflicts stop panicking and fucking call me :/
